docker compose up -d

docker ps

git clone 
cd proyecto_tesis

docker compose up -d

cd backend
npm install
npx prisma generate
npx prisma migrate dev
npm run dev

# Nueva terminal
cd frontend
npm install
npm run dev